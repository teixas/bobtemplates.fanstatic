How to use?
===========


You can import ``{{{ library.name }}}`` from ``js.{{{ library.name}}}`` 
and ``need`` it where you want these resources to be included on a page::

  >>> from js.{{{ library.name }}} import {{{ library.name }}}
  >>> {{{ library.name }}}.need()
